---
// JAVASCRIPT
// TODO: export interface Props
// - title: string
// - description: string
// - url: string
// - image: { src: string, alt: string }
// - tags: string[]

// TODO: destructure props into title, description, url, image, tags
---

<!-- HTML -->
<!-- TODO: a to url, class card -->
<!--   TODO: img class thumbnail, src image.src, alt image.alt -->
<!--   TODO: h3 class title, title -->
<!--   TODO: p class description, description -->
<!--   TODO: ul class tags -->
<!--     TODO: if tags, return <li>tag</li> for every tag -->

<!-- CSS -->
<style>
	.card {
		height: 480px;
		display: grid;
		grid-template-rows: 6fr 1fr 2fr 1fr;
		box-shadow: var(--shadow-lg);
		background-color: white;
		padding: 1rem;
		text-decoration: none;
		color: var(--dark-blue);
		min-width: 384px;
		border-radius: 1rem;
	}

	.card:hover {
		box-shadow: var(--shadow-xl);
	}

	.thumbnail {
		width: 100%;
		max-height: 100%;
		object-fit: cover;
	}

	.title {
		color: var(--dark-blue);
		font-weight: bold;
		font-size: 1.8rem;
	}

	.description {
		font-size: 1.6rem;
	}

	.tags {
		display: flex;
		list-style: none;
		grid-gap: 1rem;
		justify-content: flex-end;
	}

	.tags li {
		border: 1px solid var(--hot-pink);
		border-radius: 10rem;
		color: var(--hot-pink);
		padding: 0.2rem 0.4rem;
	}

	.tags li:hover {
		background-color: var(--hot-pink);
		color: white;
	}
</style>